import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export const server = new McpServer({
  name: "mili",
  version: "1.0.0",
  description: "this is a project to connect mili whit ia and this work in",
});
